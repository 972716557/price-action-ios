name: PriceActionScanner
options:
  bundleIdPrefix: com.yuchen
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16.0"
settings:
  SWIFT_VERSION: "5.9"
  DEVELOPMENT_TEAM: ""
targets:
  PriceActionScanner:
    type: application
    platform: iOS
    sources:
      - PriceActionScanner
    settings:
      INFOPLIST_FILE: PriceActionScanner/Resources/Info.plist
      PRODUCT_BUNDLE_IDENTIFIER: com.yuchen.PriceActionScanner
      MARKETING_VERSION: "1.0.0"
      CURRENT_PROJECT_VERSION: "1"
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
      ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
